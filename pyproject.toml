[project]
name = "webview"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pywebview>=6.0",
    "streamlit>=1.50.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.16.0",
]
